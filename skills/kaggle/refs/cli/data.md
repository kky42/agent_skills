# Data

Use the Kaggle CLI directly for competition files.

```bash
kaggle competitions files -c SLUG
kaggle competitions download -c SLUG -p PATH
kaggle competitions download -c SLUG -f FILE_NAME -p PATH
```

After download, record the source slug, retrieval time, file names, and hashes
for any data used in experiments or submissions.
